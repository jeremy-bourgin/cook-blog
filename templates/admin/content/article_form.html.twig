{% extends '/admin/content/content_form.html.twig' %}

{% block content_form %}
    {{ parent() }}

    <div class="form-group">
        {{ form_widget(form.summarize) }}
        {{ form_errors(form.summarize) }}
    </div>

    <div class="form-group">
        {{ form_widget(form.file) }}
        {{ form_errors(form.file) }}
    </div>
{% endblock %}
