{% extends 'base.html.twig' %}

{% block body %}

    {% if is_validate %}
        <p class="text-success">{{ validate_message }}</p>
        <p><a href="{{ path('cblog_admin') }}" class="btn btn-primary">Retour</a>
    {% else %}
        {{ form(form) }}
    {% endif %}

{% endblock %}
