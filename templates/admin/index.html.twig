{% extends 'base.html.twig' %}

{% block body %}
    <artcile>
        <header><h1>Gestion des articles</h1></header>
        <aside><a href="./article/add" class="btn btn-primary">Créer un article</a></aside>
        <table class="table table-striped">
            <tbody>
            {% for o in articles %}
                <tr>
                    <td>{{ o.title }}</td>
                    <td><a href="./article/update/{{ o.id }}" class="btn btn-primary">Modifier</a>
                    <td><a href="./article/delete/{{ o.id }}" class="btn btn-primary">Supprimer</a>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </article>
{% endblock %}
